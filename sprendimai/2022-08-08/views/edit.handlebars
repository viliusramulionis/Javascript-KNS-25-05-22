<form action="/edit/{{_id}}" method="POST">
    <div>
        <input type="text" name="name" placeholder="Name" value="{{name}}" />
    </div>
    <div>
        <input type="text" name="surname" placeholder="Surname" value="{{surname}}" />
    </div>
    <div>
        <input type="text" name="address" placeholder="Address" value="{{address}}" />
    </div>
    <div>
        <input type="text" name="phone" placeholder="Phone" value="{{phone}}" />
    </div>
    <div>
        <input type="email" name="email" placeholder="Email" value="{{email}}" />
    </div>
    <div>
        <input type="text" name="personalId" placeholder="Personal ID" value="{{personalId}}" />
    </div>
    <button type="submit">Siųsti</button>
</form>